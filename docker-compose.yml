services:
  meetsou:
    build: .
    image: soumeet96/personal-assistant-meetsou:latest
    ports:
      - "3000:3000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    restart: unless-stopped
